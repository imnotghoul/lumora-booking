# Local zero-configuration demo (SQLite)
DATABASE_URL="file:./dev.db"

# Replace with a long random value in production (at least 32 characters)
AUTH_SECRET="replace-with-a-long-random-secret-at-least-32-characters"

# Used when creating the seeded demo administrator
ADMIN_EMAIL="admin@lumora.ru"
ADMIN_PASSWORD="Lumora2026!"

# PostgreSQL example (use with prisma/schema.postgresql.prisma)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lumora?schema=public"
